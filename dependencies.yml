version: 2
dependencies:
- type: js
  settings:
    commit_message_prefix: "chore(package): "
    related_pr_behavior: close
    github_labels:
    - dependencies
  manifest_updates:
    filters:
    - name: "@angular/.*"
      versions: L.Y.Y
